apply plugin: 'java'
apply plugin: 'application'

mainClassName = "hopur0.games.toetactic.Game"

sourceSets {
    main.java.srcDir "src/main"
    test.java.srcDir "src/test"
}

repositories {
    mavenCentral()
}

dependencies {
     testCompile group: 'junit', name: 'junit', version: '4.+'
}
